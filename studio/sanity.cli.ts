import { defineCliConfig } from "sanity/cli";

export default defineCliConfig({
  api: { projectId: "j4cazn05", dataset: "production" },
  studioHost: "designedbyalok",
  deployment: { appId: "sn9kyozid5oco2h1jugmbidt" },
});
