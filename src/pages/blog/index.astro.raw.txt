---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog", ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<style>
  .post-entry {
    padding: 3rem 0;
  }

  .post-entry:first-child {
    padding-top: 0;
  }

  .post-entry:last-child {
    padding-bottom: 0;
  }

  .post-entry hr {
    margin: 0;
  }

  .post-entry .thumbnail {
    aspect-ratio: 3 / 2;
    background: var(--paper-deep);
    border: 1px solid var(--rule);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .post-entry .thumbnail .eyebrow {
    color: var(--ink-faint);
  }

  .post-entry .content {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .post-entry .thumbnail-container {
    flex: 0 0 42%;
  }

  .post-entry .text-container {
    flex: 0 0 58%;
  }

  @media (max-width: 768px) {
    .post-entry .content {
      flex-direction: column;
    }

    .post-entry .thumbnail-container {
      flex: 0 0 auto;
    }

    .post-entry .text-container {
      flex: 0 0 auto;
    }
  }
</style>

<Layout title="Blog — Alok Kumar" description="Notes from the desk.">
  <div class="container-edge container-content">
    <div style="padding-top: 5rem;">
      <div class="eyebrow">Notes</div>
      <div class="display text-balance">Things I've been writing about.</div>
    </div>

    <div style="margin: 4rem 0;"></div>

    {posts.map((post, index) => (
      <div class="post-entry" key={post.id}>
        {index > 0 && <hr />}
        <div class="content">
          <div class="text-container">
            <div class="eyebrow">
              {post.data.date.toLocaleDateString("en-US", { year: "numeric", month: "long", day: "numeric" })}
              {post.data.readTime && ` · ${post.data.readTime}`}
            </div>
            <h2 class="display display-sm" style="font-weight: 420;">
              <a class="link" href={`/blog/${post.id}`} style="color: var(--ink);">
                {post.data.title}
              </a>
            </h2>
            <p class="lede text-pretty" style="color: var(--ink-muted); max-width: 55ch;">
              {post.data.description}
            </p>
            <div style="display: flex; gap: 0.5rem; flex-wrap: wrap; margin-bottom: 1rem;">
              {post.data.tags.map((tag) => (
                <span class="eyebrow" style="font-size: 0.75rem; padding: 0.15rem 0.5rem; border: 1px solid var(--rule); border-radius: 999px; color: var(--ink-muted);">
                  {tag}
                </span>
              ))}
            </div>
            <a class="link link-accent" href={`/blog/${post.id}`}>
              Read on →
            </a>
          </div>
          <div class="thumbnail-container">
            {post.data.thumbnail ? (
              <div class="thumbnail">
                <img src={post.data.thumbnail.src} alt="" style="width: 100%; height: 100%; object-fit: cover;" />
              </div>
            ) : (
              <div class="thumbnail">
                <div class="eyebrow">thumbnail</div>
              </div>
            )}
          </div>
        </div>
      </div>
    ))}

    <hr />
    <div style="margin: 6rem 0;"></div>
  </div>
</Layout>